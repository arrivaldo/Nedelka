import React from 'react'

const Loading = () => {
  return (
        <section style={{display: 'grid', placeItems: 'center', height: '100vh', width: '100%'}}>
            <h1 className='text-4xl' >Loading...</h1> 
        </section>
    )
}

export default Loading